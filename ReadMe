
Mars rovers exercise.

Download source.

RUN ‘npm install’ and ‘bower install’ to set up all packages in the template. (Requires node.js)


Open src/index.html in a browser and follow instructions.  (Requires an http server.  If you've got node
install with 'node install http-server -g'; then open terminal to the root of this source and run 'http-server'

